# Zip and unzip forecasts by forecast date

Zip and unzip forecasts by forecast date

## Usage

``` r
zip_unzip(type = NULL, forecast_path = "forecasts/", date = NULL)
```

## Arguments

- type:

  either zip or unzip

- forecast_path:

  location of forecast directory

- date:

  forecast date to use while zipping
