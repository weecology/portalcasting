function outline:

-setup_dir
  -create_dir
    -create_main_dir
    -create_sub_dirs
      -create_sub_dir
  -fill_dir 
    -fill_PortalData
	  -download_observations
    -fill_data
	  -prep_moons
	  -prep_rodents
	  -prep_covariates
	  -prep_metadata
	-fill predictions
	-fill_models
	  -write_model





directory tree:

-base (existing folder where the project folder will be housed)
-main (project folder encompassing all subfolders)
-subs (specific sub-folders for the project)


options list:

-options_all
  -options_dir
  -options_data
    -moons
    -rodents
    -covariates
    -metadata
  -options_predictions
  -options_models
  -options_casts
  
  
portalcast:

the main function that triggers the forecast or hindcast process as outlined
by the options. 


TO DO:

connect the historical covariate forecasts back around
make fill_predictions only download what is new (like everything else needs to do)

